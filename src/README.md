This folder no longer holds the live browser JavaScript runtime.

Current use:

- CSS source files used by the site
- small reference notes that still belong with those assets

JavaScript/TypeScript source of truth now lives in `src/ts/`.
Compiled browser JavaScript now lives in `static/`.
